import React, { Component } from 'react';
import { View } from 'raect-native';

export default class SearchBar extends Component {
    render() {
        return (
            <View/>
        )
    }
}