syntax = "proto3";

package tag;

service Tag {
    rpc GetTags (TagRequest) returns (TagResponse){}
}

message TagRequest {
    string description = 1;
}

message TagResponse {
    repeated string tags = 1;
}
